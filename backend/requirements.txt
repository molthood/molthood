# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation & settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Persistence (prepared, not connected in Phase 3) ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
redis==5.2.1

# --- Outbound HTTP (clients are stubs in Phase 3) ---
httpx==0.28.1

# --- Content parsing (web intelligence) ---
beautifulsoup4==4.15.0
lxml==6.1.1
feedparser==6.0.12
pypdf==6.14.2
markdownify==0.14.1
readability-lxml==0.8.1

# --- Error tracking (optional) ---
# Without SENTRY_DSN the SDK is never initialised, so this is dead weight on a
# deployment that does not want it — and one line to remove.
sentry-sdk[fastapi]==2.20.0

# --- Logging ---
structlog==24.4.0

# --- Sandboxed code execution (optional) ---
# Only needed when E2B_API_KEY is set. The provider reports the package as
# missing rather than failing to import, so a deployment that never runs code
# can drop this line.
e2b-code-interpreter==2.9.0
